# =============================================================================
# Reachly — environment configuration
# Copy to `.env` at the repo root. Never commit real secrets.
# Every provider below is optional: when credentials are missing the app either
# falls back to a clearly-labelled mock provider (DEMO_MODE=true) or shows a
# "Connect provider" state. Nothing pretends to work without credentials.
# =============================================================================

# ---- Runtime -----------------------------------------------------------------
NODE_ENV=development
# development | staging | production
APP_ENV=development
APP_URL=http://localhost:3000
# false = everything runs for real (recommended, and required in production).
# true = missing providers fall back to mock adapters and the UI shows a persistent
# "Demo mode" indicator — only for exploring the seeded demo workspace. The e2e suite
# turns it on for its own server, so leave it false here.
DEMO_MODE=false
# Simulate inbound provider events (deliveries, opens, replies, call outcomes)
# for mock providers. Only used when DEMO_MODE=true.
DEMO_SIMULATE_EVENTS=false
LOG_LEVEL=debug

# ---- Database (PostgreSQL) -----------------------------------------------------
DATABASE_URL=postgresql://reachai:reachai@localhost:54329/reachai?schema=public
# Direct (non-pooled) connection used by migrations. Same as DATABASE_URL locally.
DIRECT_URL=postgresql://reachai:reachai@localhost:54329/reachai?schema=public
# Used by integration tests (created by infra/docker/postgres/init.sql).
TEST_DATABASE_URL=postgresql://reachai:reachai@localhost:54329/reachai_test?schema=public

# ---- Redis (queues, rate limits, caching) --------------------------------------
REDIS_URL=redis://localhost:6379
# bullmq = Redis-backed queues processed by apps/worker (production).
# inline = jobs run inside the web process with the same retry/backoff policy — for local
#          development on machines without Redis. Never use inline in production.
QUEUE_DRIVER=bullmq

# ---- Security ------------------------------------------------------------------
# 32+ char random strings. Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
BETTER_AUTH_SECRET=replace-with-a-long-random-string-at-least-32-chars
# Key used to encrypt provider credentials at rest (AES-256-GCM). 32 bytes, base64.
ENCRYPTION_KEY=replace-with-32-byte-base64-key
# Signs unsubscribe links and internal callback tokens.
SIGNING_SECRET=replace-with-another-long-random-string

# ---- Authentication ------------------------------------------------------------
AUTH_REQUIRE_EMAIL_VERIFICATION=false
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ---- AI providers --------------------------------------------------------------
# mock | openai | anthropic | google
AI_DEFAULT_PROVIDER=mock
# Optional overrides; defaults are defined in packages/config/src/ai-models.ts
AI_DEFAULT_MODEL=
AI_FALLBACK_MODEL=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_AI_API_KEY=
# Cache identical AI requests (seconds). 0 disables.
AI_CACHE_TTL_SECONDS=86400

# ---- Lead data providers -------------------------------------------------------
# openstreetmap (real businesses, free, no key) | google_places (needs GOOGLE_PLACES_API_KEY) | mock (generated demo data)
LEAD_PROVIDER=openstreetmap
GOOGLE_PLACES_API_KEY=
# Public website enrichment (fetches homepage only, honours robots.txt)
ENRICHMENT_WEBSITE_FETCH_ENABLED=true

# ---- Email ---------------------------------------------------------------------
# mock | resend | sendgrid | smtp
EMAIL_PROVIDER=mock
EMAIL_FROM="Reachly <outreach@example.com>"
RESEND_API_KEY=
RESEND_WEBHOOK_SECRET=
SENDGRID_API_KEY=
SENDGRID_WEBHOOK_PUBLIC_KEY=
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_SECURE=false

# ---- WhatsApp (official Meta WhatsApp Business Cloud API only) -----------------
# mock | meta
WHATSAPP_PROVIDER=mock
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_BUSINESS_ACCOUNT_ID=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_APP_SECRET=
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_GRAPH_API_VERSION=v21.0

# ---- Voice ---------------------------------------------------------------------
# mock | twilio | vapi — see docs/calling.md. Webhooks need a public APP_URL.
VOICE_PROVIDER=mock
# Twilio: places the call; the conversation runs turn by turn via /api/webhooks/voice/twilio/turn
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
# Vapi: hosted voice agent; server URL /api/webhooks/voice/vapi, secret sent as x-vapi-secret
VAPI_API_KEY=
VAPI_PHONE_NUMBER_ID=
VAPI_WEBHOOK_SECRET=

# ---- Billing -------------------------------------------------------------------
# Stripe: cards, auto-renewing subscriptions and the customer portal. Only the secret key is
# required — prices are created in your Stripe account on first checkout. For renewals and
# cancellations, point a webhook at <APP_URL>/api/webhooks/stripe and paste its signing secret.
# Locally: `stripe listen --forward-to localhost:3000/api/webhooks/stripe`.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Optional: use existing Stripe prices instead of the ones Reachly creates.
STRIPE_PRICE_PRO_MONTHLY=
STRIPE_PRICE_SCALE_MONTHLY=
# Razorpay: UPI (and Indian cards/netbanking) in INR. Each payment buys one month; there is
# no auto-debit. Test keys (rzp_test_…) work with the UPI id success@razorpay.
# Webhook (optional, recommended): <APP_URL>/api/webhooks/razorpay, events payment.captured + payment.failed.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=

# ---- n8n -----------------------------------------------------------------------
# Set to http://localhost:5678 after `npm run infra:up` (empty = n8n steps are simulated in demo mode).
N8N_URL=
# n8n → Settings → n8n API; lets Reachly list your workflows.
N8N_API_KEY=
# Signs Reachly → n8n calls (x-reachai-signature); set the same value as REACHAI_WEBHOOK_SECRET in n8n.
N8N_WEBHOOK_SECRET=replace-with-a-shared-secret

# ---- Storage (S3-compatible) ---------------------------------------------------
# local | s3
STORAGE_DRIVER=local
STORAGE_LOCAL_DIR=.storage
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_BUCKET=reachai
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_FORCE_PATH_STYLE=true

# ---- Notifications -------------------------------------------------------------
SLACK_WEBHOOK_URL=
